<!DOCTYPE html>
  <head>
    <title>Simple Map</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script> 
    <meta name="viewport" content="initial-scale=1.0, user-scalable=no">
    <meta charset="utf-8">
    <style>
      html, body, #map-canvas {
        height: 100%;
        margin: 0px;
        padding: 0px
      }
      body{
        overflow: hidden;  /*no scrollbar nonsense*/ 
      }

      /*.pano{
      	display:block;
      	float:right;
      }*/
      #image-container {
 /*   border: 2px dashed #444;
    height: 125px;*/
    
    text-align: justify;
    -ms-text-justify: distribute-all-lines;
    text-justify: distribute-all-lines;
    
    /* just for demo */
    /*min-width: 612px;*/
}

.box {
    width: 19.5%;
    height: 100%;
    vertical-align: top;
    display: inline-block;
    *display: inline;
    zoom: 1;

}
.stretch {
    width: 100%;
    display: inline-block;
    font-size: 0;
    line-height: 0;
}

/*.box1, .box2, .box3, .box5, .box4 {
    background: #ccc
}*/


      
      
    </style>
    <script src="https://maps.googleapis.com/maps/api/js"></script>
    <script>
      var map;
      var marker;
      var markerArray = [];
      var panArray = [];
      var geocoder;
      var divCount = 0;

      function createInfoWindow(text){
      	var infowindow = new google.maps.InfoWindow({
      		content: text
      	})
      	return infowindow;
      }


      // creates new marker given lat/long of wherever you click, then pans to that new marker. 
      function createMarker(coords, map) {
        marker = new google.maps.Marker({
          position: coords,
          map: map
        });
        markerArray.push(marker);
        map.panTo(marker.position);
        //var info = createInfoWindow(marker.title);

        // for each marker if you click it, you get the panorama street view image, which appears
        // in the "pano" div at the bottom of the page in the image container. 
        google.maps.event.addDomListener(marker,'click', function(event){   
          var panoramaOptions = {
  			   position: coords,
  			   pov: {
    			   heading: 0,
    			   pitch: 0
  					   },
           scrollwheel: false,
           linksControl: false,
           panControl: false,
           addressControlOptions: {
              position: google.maps.ControlPosition.TOP_RIGHT
              }
			     };
         
           // tests to see if street view panorama exists before displaying image in image-container.
           var svTest = new google.maps.StreetViewService();
           svTest.getPanoramaByLocation(coords, 50, function(data,status){
            if(status == "OK"){
               
               
               if( $("#image-container").css('display', 'none')){
                    $('#image-container').animate({ 'bottom' : '0px' });     
                    $("#image-container").toggle();
                    var divID = "pano" + divCount;
                    $( "#image-container" ).prepend( "<div class='box' id="+divID+"></div>" );
                    var panorama = new  google.maps.StreetViewPanorama(document.getElementById(divID), panoramaOptions); 
                   // if ($('#pano1').children().length == 0){
                   //  var panorama = new  google.maps.StreetViewPanorama(document.getElementById("'pano'+divCount"), panoramaOptions);      
                   //  }
                   //  else if ($('#pano2').children().length == 0){
                   //  var panorama = new  google.maps.StreetViewPanorama(document.getElementById("pano2"), panoramaOptions);  	
                   //  }
                   //  else if ($('#pano3').children().length == 0){
                   //  var panorama = new  google.maps.StreetViewPanorama(document.getElementById("pano3"), panoramaOptions);  	
                   //  }  
                   //  else if ($('#pano4').children().length == 0){
                   //  var panorama = new  google.maps.StreetViewPanorama(document.getElementById("pano4"), panoramaOptions);  	
                   //  }
                   //  else{
                   //  var panorama = new  google.maps.StreetViewPanorama(document.getElementById("pano5"), panoramaOptions);  	
                   //  }
                    panArray.push(panorama);
                    divCount++;
            }
            }
            else{
              var contentString = "No Street View Available";
              var infowindow = new google.maps.InfoWindow({
                content: contentString
                });
              infowindow.open(map,marker);


            }
           });
          

           // on marker click scroll up image-container if image-container is hidden
           
            //$("#map-canvas").height("75%"); 
        })
      }

      // sets up the map object using mapOptions properties, creates 'home base' marker, and adds listener to the map object
      // so that on click you create a new marker.
      function initialize() {
        detectBrowser();
        var mapOptions = {
          zoom: 10,
          center: new google.maps.LatLng(37.736414, -122.247953)
        };
        map = new google.maps.Map(document.getElementById('map-canvas'),
            mapOptions);
        createMarker({lat: 37.736414, lng: -122.247953}, map);

        google.maps.event.addDomListener(map, 'click', function(event){
            createMarker(event.latLng, map);
        });
      }

      google.maps.event.addDomListener(window, 'load', initialize);

    </script>
  </head>
  <body>

    <div id="map-canvas" style="height:100%;width:100%;"></div>
    <div id="image-container" style="background-color:#333333;height:25%;width:100%;box-shadow: 0px -3px 6px rgba(0,0,0,0.4);bottom:-100px; position:absolute;display:none">
    <!-- <div class="box"id="pano1" ></div>
    <div class="box"id="pano2" ></div>
    <div class="box"id="pano3" ></div>
    <div class="box"id="pano4"></div>
    <div class="box"id="pano5"></div> -->
    <span class="stretch"></span>
    </div>
  </body>
</html>






